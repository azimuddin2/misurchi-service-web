const envConfig = {
  baseUrl: process.env.NEXT_PUBLIC_BACKEND_BASEURL,
  imageUrl: process.env.NEXT_PUBLIC_IMAGE_URL,
  socketApi: process.env.NEXT_PUBLIC_SOCKET_BASEURL,
};

export default envConfig;
